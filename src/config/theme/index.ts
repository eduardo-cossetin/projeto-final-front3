import { createTheme } from "@mui/material";

const meuTema = createTheme({
  palette: {
    primary: {
      main: "#1e3b37",
      light: "#2a524d",
      dark: "#122421",
      contrastText: "white",
    },
  },
});

export default meuTema;
